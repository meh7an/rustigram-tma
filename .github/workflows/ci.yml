name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v6
        with:
          version: 10

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          check-latest: true
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm -r --filter './packages/**' typecheck

      - name: Build
        run: pnpm -r --filter './packages/**' build

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm exec vitest run # uses vitest.workspace.ts — runs all three packages
